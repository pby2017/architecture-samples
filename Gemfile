source 'https://rubygems.org'
gem "fastlane" # The easiest way to automate beta deployments and releases for your iOS and Android apps

######## fastlane 설치 중 필요한 unf_ext(0.0.8.2) 설치를 위해 기본 ruby 버전(2.6.0)을 rbenv로 설치한 새 ruby 버전(2.7.6)으로 변경
#### rbenv 설치
# brew install rbenv
#### rbenv 초기화
# echo '# rbenv' >> ~/.zshrc
# echo 'if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi' >> ~/.zshrc
# source ~/.zshrc
#### rbenv 으로 설치할 수 있는 ruby 버전 확인
# rbenv install -l
#### ruby 설치
# rbenv install 2.7.6 && rbenv rehash
#### 필요에 따라 ruby 지정
# rbenv global 2.7.6